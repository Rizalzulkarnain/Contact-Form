import styled from 'styled-components';

import theme from '../theme';

export const TitleContactForm = styled.h1`
  text-align: center;
  font-size: ${theme.fontSizes.xl};
  font-weight: 500;
  color: ${theme.colors.white};
  padding-bottom: ${theme.spacing.md};
`;
